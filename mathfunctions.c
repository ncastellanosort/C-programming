#include <stdio.h>
// Usar esta biblioteca para las funciones matematicas
#include <math.h>

int main()
{
  double A = sqrt(9);
  double B = pow(2,4);
  int C = round(3.14);
  // ceil lo redondea al decimal mas alto
  int D = ceil(3.14);
  // floor al mas alto
  int E = floor(3.14);
  double F = fabs(-100);
  double G = log(3);
  double H = sin(35); //tan(), cos()

  return 0;
}
